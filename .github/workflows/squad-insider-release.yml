name: Squad Insider Release
# Project type was not detected — configure build, test, and insider release commands below

on:
  push:
    branches: [insider]

permissions:
  contents: write

jobs:
  release:
    runs-on: ${{ fromJSON(vars.RUNS_ON_LINUX_PERSONAL || '["self-hosted","personal-linux"]') }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Build and test
        run: |
          # TODO: Project type was not detected — add your build/test commands here
          # Go:            go test ./...
          # Python:        pip install -r requirements.txt && pytest
          # .NET:          dotnet test
          # Java (Maven):  mvn test
          # Java (Gradle): ./gradlew test
          echo "No build commands configured — update squad-insider-release.yml"

      - name: Create insider release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # TODO: Add your insider/pre-release commands here
          echo "No release commands configured — update squad-insider-release.yml"
