# Orchestration Log: iris-59-cert

**Timestamp:** 2026-05-13T12:03:00Z  
**Agent:** iris  
**Model:** Haiku  
**Task:** Graph API SPN certificate auth walkthrough (issue #59)

## Routing & Execution

**Why chosen:** Iris owns Microsoft Graph integration research — domain expert on Entra ID, authentication patterns, and PowerShell Microsoft.Graph module.

**Mode:** Autonomous  
**Duration:** ~25 minutes  
**Branch:** squad/59-graph-cert-auth-2026-05-13

## Deliverable

**Output:** PR #63 — Graph SPN cert-auth walkthrough in PERMISSIONS.md
- Confirmed Invoke-GraphApi.ps1 auth parameters and scopes:
  - `-TenantId` (optional; defaults to Az context)
  - `-ClientId` (SPN application ID)
  - `-CertificatePath` (path to PFX file)
  - `-UseIdentity` and `-UseDeviceCode` switches
- Verified scopes required:
  - Policy.Read.All (Conditional Access policies)
  - RoleManagement.Read.Directory (PIM eligible roles)
  - Directory.Read.All (break-glass user detection)
- Walkthrough added to PERMISSIONS.md with example workflow steps

**Files produced:**
- Modified: PERMISSIONS.md (Graph SPN cert auth section + example workflow)
- Modified: .squad/agents/iris/history.md (auth pattern learning)

## Outcome

✓ PR #63 merged — auth documentation complete
✓ Graph scope mapping confirmed and documented
✓ SPN workflow example ready for operators

## Race Condition Note

**Incident:** Iris's branch squad/59-graph-cert-auth ran in parallel with Sage's squad/57-coverage. When Iris checked out squad/59, it picked up Sage's squad/57 commit via shared-checkout state (same working tree). Coordinator rebased squad/59 to drop the duplicate before merge.

**Git history:** Iris's commit included Sage's changes; after rebase, squad/59 contained only Iris's PERMISSIONS.md changes.

**Lesson:** Parallel agents in same working tree must coordinate git state. Worktrees recommended for future rounds.

## Notes

- SPN cert auth is foundational for unattended automation (CI/CD pipelines)
- Scopes are subset of what Invoke-GraphApi.ps1 can request — auth layer is flexible
